#include <stdio.h>

int disp_reverse_number(int n)
{
    int rev = 0;

    while(n > 0)
    {
        rev = rev * 10 + (n % 10);
        n = n / 10;
    }

    return rev;
}

int main()
{
    int n, ans;

    scanf("%d", &n);

    ans = disp_reverse_number(n);

    printf("%d", ans);
}
