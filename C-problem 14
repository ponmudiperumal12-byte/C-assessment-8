#include <stdio.h>

int disp_single _digit_square(int n)
{
    int digit, count = 0;

    while(n > 0)
    {
        digit = n % 10;

        if(digit == 0 || digit == 1 || digit == 4 || digit == 9)
            count++;

        n = n / 10;
    }

    return count;
}

int main()
{
    int n, total;
    printf("enter the number:) ;
    scanf("%d", &n);

    total= disp_single_digit_square(n);

    printf("%d", ans);

}
