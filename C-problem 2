#include <stdio.h>

int disp_rsum()
{
    int i, s = 0;

    for(i = 6; i >= 1; i--)
    {
        s = s + i;
    }

    return s;
}

int main()
{
    int ans;

    ans = disp_rsum();

    printf("%d", ans);
}
